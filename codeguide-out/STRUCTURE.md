# CodeGuide Structure — santhosh.raju

Module dependency graph. Read this to understand how the codebase fits together.

## Module dependencies

**components** — no internal dependencies (foundational module)
**docs** — no internal dependencies (foundational module)
**layouts** — no internal dependencies (foundational module)
**pages** — no internal dependencies (foundational module)
**public** — no internal dependencies (foundational module)
**scripts** — no internal dependencies (foundational module)
**src** — no internal dependencies (foundational module)
**styles** — no internal dependencies (foundational module)

## Start here

When learning this codebase, a good reading order is:

1. `src/components/` — UI components (no deps, foundational)
1. `docs/` — Code in docs/ (no deps, foundational)
1. `src/layouts/` — Code in src/layouts/ (no deps, foundational)
1. `src/pages/` — Page components (no deps, foundational)
1. `public/` — Code in public/ (no deps, foundational)
1. `scripts/` — Scripts and tooling (no deps, foundational)
1. `src/` — Code in src/ (no deps, foundational)
1. `src/styles/` — Code in src/styles/ (no deps, foundational)
